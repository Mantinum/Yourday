# yourday

Agenda intelligent qui recommande et envoie des cadeaux.

## Démarrage

```bash
pnpm install
docker-compose up -d
pnpm prisma migrate dev
pnpm -w dev
```

API disponible sur `http://localhost:4000/health`.
