{
  "name": "Yourday",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:20",
  "dockerComposeFile": ["../docker-compose.dev.yml"],
  "runServices": ["postgres", "redis"],
  "forwardPorts": [4000, 3000],
  "postCreateCommand": "corepack enable && pnpm install && pnpm prisma generate && pnpm prisma migrate dev -n sprint3_boot && pnpm exec ts-node prisma/seed.ts"
}
