{
  "name": "yourday",
  "private": true,
  "engines": { "node": ">=20" },
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "echo 'todo: run all apps'",
    "build": "echo 'build placeholder'",
    "test": "echo 'test placeholder'"
  }
}
