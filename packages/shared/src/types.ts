export enum Preferences {
  Email = 'email',
  Sms = 'sms',
}

export enum Product {
  GiftCard = 'gift_card',
  Physical = 'physical',
}

export enum OrderStatus {
  Created = 'created',
  Sent = 'sent',
  Failed = 'failed',
}
